#!/usr/bin/env bash
# Format Rust code and regenerate Tailwind CSS before each commit.

cargo fmt --all
git add -u '*.rs'

tailwindcss -i tailwind.input.css -o crates/server/public/tailwind.css --minify 2>/dev/null
git add crates/server/public/tailwind.css
