//! axum + Maud/HTMX server. Populated in milestone 5.

#[tokio::main]
async fn main() -> anyhow::Result<()> {
    tracing_subscriber::fmt::init();
    tracing::info!("lineup_server stub — implement in milestone 5");
    Ok(())
}
